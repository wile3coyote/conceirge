<resources>
    <!-- Dark mode shell theme. Compose handles all theming. -->
    <style name="Theme.Conceirge" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
